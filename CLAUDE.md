# 이 폴더 작업 지침 (CLAUDE.md)

> 이 파일은 Claude가 이 폴더에서 작업을 시작할 때마다 자동으로 읽는 "작업 약속"입니다.
> 사용자는 **비개발자**이며 개발을 연습하는 중입니다.

## 이 폴더는 무엇인가요?

- **개발 연습용 폴더**입니다. 완벽함보다 "직접 해보며 배우기"가 목적이에요.
- 사용하는 기술:
  - **Next.js** — 웹사이트(화면 + 기능)를 만드는 도구입니다. (요리로 치면 '주방')
  - **Supabase** — 회원정보·상품 같은 데이터를 보관하는 **온라인 데이터베이스 + 로그인 기능** 묶음입니다. (요리로 치면 '냉장고 + 창고')
  - **Vercel** — 만든 웹사이트를 **인터넷에 올려 남들도 볼 수 있게** 해주는 배포 서비스입니다. (요리로 치면 '손님에게 내놓는 식당')

## 지금 이 폴더에 있는 것

1. **가계부** — `index.html` 파일 하나로 된 간단한 웹페이지. 이미 Vercel로 배포돼 있어요.
2. **고구마마켓 (`gogumamarket/` 폴더)** — Next.js로 만드는 중고거래 사이트. (회원가입/로그인/상품등록까지 완료)
   - 가계부와 **같은 Supabase**를 함께 쓰고, 배포는 **나중에 따로** 할 예정입니다.

## 작업할 때 꼭 지킬 규칙

1. **쉽게 설명하기**
   - 개발 용어가 나오면 항상 비개발자가 이해할 수 있게 쉬운 말/비유로 풀어서 설명합니다.

2. **진행 상황 자주 알리기**
   - "지금 무엇을 하는 중인지"를 작업 중간중간 자주 이야기합니다.
   - 한 번에 많은 걸 말없이 진행하지 않습니다.

3. **시킨 것만, 단계적으로**
   - 사용자가 **지시한 것만** 수행합니다. 미리 앞서나가 다음 기능까지 만들지 않습니다.
   - 큰 작업은 작은 단계로 나눠서 하나씩 진행합니다.

4. **사람이 할 일은 최소화하고, 친절히 안내**
   - Claude가 할 수 있는 작업(코드 작성, 데이터베이스 설정, 실행, 확인 등)은 **최대한 직접** 처리합니다.
   - 꼭 **사람이 직접 해야 하는 일**(예: 비밀번호 입력, 외부 사이트 버튼 클릭, 결제, 사진 업로드 등)만 골라서,
     **무엇을 / 어디서 / 어떻게** 하면 되는지 단계별로 쉽게 안내합니다.

5. **끝나면 바로 보여주기**
   - 개발이 끝나면 항상 **결과를 바로 확인할 수 있게** 화면을 열어줍니다.
     (개발 서버를 실행하고 브라우저 미리보기로 보여주거나, 어디서 확인하면 되는지 알려줍니다.)

## 참고 메모

- 고구마마켓 실행: `gogumamarket` 폴더에서 `npm run dev` (로컬 주소로 미리보기).
- 데이터베이스(Supabase) 변경은 Claude가 직접 처리할 수 있어요.
- Next.js 16 기준이라 일부 옛 방식과 다를 수 있습니다 (예: `middleware` → `proxy`).
